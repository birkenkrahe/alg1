#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>

int main(int argc, char **argv)
{
  char *name;
  name = (char *) malloc(strlen(argv[1]) + 1);
  strcpy(name,argv[1]);
  printf("Hello, %s!\n",name);
  free(name)
  return 0;
}
